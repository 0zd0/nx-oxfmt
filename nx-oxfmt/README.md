# nx-oxfmt

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-oxfmt` to build the library.

## Running unit tests

Run `nx test nx-oxfmt` to execute the unit tests via [Jest](https://jestjs.io).
